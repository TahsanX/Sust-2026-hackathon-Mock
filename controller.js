export const sortTicketHandler = (req, res) => {
    const {ticket_id, channel, locale, message} = req.body;
    // Ai er bhitor channel, locale, message pass korabi then ask korbi oita kon category te pore. oi response er upor base kore ami repponse pathabo.
}
